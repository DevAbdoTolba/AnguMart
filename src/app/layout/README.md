Componenets

Any non-main content layouts in the app (nav-bar/side-bar/hero-section/footer)

must be eager loading. every user has to be loadded jsut as the user lands on the site (Search well in which would be lazy and which would be eager)