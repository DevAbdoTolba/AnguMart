Componenets OR native TS logic OR Directive OR Pipes ;-;

repeated useage for DRY code. (re-usable)

Shared same logical approch (functions)

Shared same datatypes/componentes in multpile layouts

Forms for example (ReactiveFormsModule <- Better /TemplateDriven)
